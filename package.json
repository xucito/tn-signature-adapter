{
  "name": "@turtlenetwork/signature-adapter",
  "version": "2.0.4",
  "types": "src/index.ts",
  "main": "dist/index.js",
  "dependencies": {
    "@turtlenetwork/data-entities": "^7.0.0",
    "@turtlenetwork/ledger": "^0.1.5",
    "@turtlenetwork/signature-generator": "^1.0.1"
  },
  "scripts": {
    "compile": "tsc",
    "browserify": "node_modules/.bin/browserify dist/index.js -s wavesSignatureAdapter -u @turtlenetwork/data-entities -u @turtlenetwork/signature-generator -u @turtlenetwork/ledger -o dist/signature-adapter.js",
    "build": "npm run compile && npm run browserify && npm run uglify",
    "uglify": "node_modules/.bin/uglifyjs dist/signature-adapter.js -o dist/signature-adapter.min.js",
    "postversion": "npm publish",
    "prepare": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "browserify": "^16.2.2",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.3",
    "typescript": "^2.9.2",
    "uglify-js": "^3.4.7"
  },
  "jest": {
    "collectCoverage": true,
    "testEnvironment": "jest-environment-node",
    "coveragePathIgnorePatterns": [
      "/node_modules/",
      "/test/"
    ],
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "testMatch": [
      "**/test/*.+(ts|tsx|js)"
    ]
  }
}
